server:
  port: 8080
  servlet:
    context-path: /api/v1

spring:
  application:
    name: billetera-digital-api

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=billeteraDigitalDB;encrypt=true;trustServerCertificate=true;
    username: sa
    password: 2026@app!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update # Útil en desarrollo; se ajustará en fases posteriores si se requiere
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.SQLServerDialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.billetera.api: DEBUG